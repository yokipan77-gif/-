# 复制本文件为 .env 后填写（不要把 .env 上传到网盘或 git）
# 可放在：① 桌面  ② 与本程序同一文件夹（与 open_seat.py 同级）
FUDAN_STUDENT_ID=你的学号
FUDAN_LIB_PASSWORD=你的密码

# 可选：座位区域 ID（与网址里 area= 一致）。文科馆四层若不是 3，在浏览器里点选楼层后看地址栏再改这里。
# FUDAN_SEAT_AREA_ID=3
